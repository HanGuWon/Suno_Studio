{
  "asset_import": {
    "required_fields": ["normalizeOnImport", "file"],
    "content_type_prefix": "multipart/form-data; boundary="
  },
  "audio_job": {
    "required_fields": ["clientRequestId", "prompt", "metadata"],
    "optional_fields": ["assetId", "file", "providerMode"],
    "content_type_prefix": "multipart/form-data; boundary="
  },
  "manual_complete": {
    "required_fields": ["mixFiles", "stemFiles", "tempoLockedStemFiles", "midiFiles"],
    "content_type_prefix": "multipart/form-data; boundary="
  }
}
